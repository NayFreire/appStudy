import React, { Component } from 'react'
import { Image } from 'react-native'

const ImageEmptyList = () => (
   <Image style={{alignSelf: 'center'}}
   source = {require('../images/emptyList.png')} />
)
export default ImageEmptyList